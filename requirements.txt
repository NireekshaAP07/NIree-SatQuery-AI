# SatQuery AI — pinned backend dependencies

# API & Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==23.0.0
python-multipart==0.0.12
websockets==13.1
httpx==0.28.1

# Config & Validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Database (PostgreSQL + PostGIS)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]
geoalchemy2==0.15.2
alembic==1.13.3

# Cache & Job Queue
redis==5.1.1
tenacity==8.5.0

# Storage (S3 / MinIO Distributed Object Storage)
boto3==1.35.36

# Agent Framework
langgraph>=0.2.35
langchain>=0.3.3
langchain-core>=0.3.12
langchain-openai>=0.2.3   # GPT-4o orchestrator (best production LLM)

# ── MODEL INJECTION POINT ────────────────────────────────────────────────────
# PyTorch / local VLM packages are in requirements-gpu.txt.
# For the API-based MVP (Gemini), they are NOT needed here.
# The model_provider.py gracefully stubs the VLM when VLM_MODEL_NAME is
# set to the default placeholder (see app/core/model_provider.py:51-56).
# Install requirements-gpu.txt only if you want to run a local HuggingFace VLM.
# ─────────────────────────────────────────────────────────────────────────────

# Computer Vision
opencv-python-headless==4.10.0.84
Pillow==10.4.0
numpy
scipy==1.14.1

# Geospatial / GIS (GDAL installed via Dockerfile apt-get)
rasterio==1.3.11
geopandas==1.0.1
shapely==2.0.6
pyproj==3.6.1
fiona==1.10.1
affine==2.4.0

# Gemini VLM (Specialist Agents — Option A integration from satquery_backend)
# If GEMINI_API_KEY is not set, GeminiVisionClient runs in offline/heuristic mode.
# google-genai >=2.0 and langchain-google-genai >=4.0 both support the new AQ. key format.
google-genai>=1.4.0
langchain-google-genai>=4.0.0

# Logging & Observability
structlog==24.4.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Reports & Export
fpdf2==2.8.1
