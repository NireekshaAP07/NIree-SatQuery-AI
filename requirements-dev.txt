# Development & testing dependencies for SatQuery AI.
# Install with: pip install -r requirements-dev.txt
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
pytest-env>=1.1
httpx>=0.27          # Async HTTP client for integration tests (ASGI transport)
anyio>=4.4
aiosqlite>=0.20      # In-memory async SQLite for offline unit tests (no Postgres needed)
pip-audit>=2.8        # Security dependency vulnerability scanner
